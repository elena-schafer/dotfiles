Install antigen using the following method
> curl -L git.io/antigen > antigen.zsh
File should go into home directory

Install fzf by running
> git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
then the install script
> ~/.fzf/install
and accept all option
